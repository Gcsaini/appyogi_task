#1 first create the database and update that db name into connection.php file
#2 create tables with names as buttons and control or you can import .sql files to database
#3 use this url to test the application "https://localhost/root_folder/?user=1" for localhost
# db files are in the db folder